ext {
    android = [
            compileSdkVersion: 28,
            buildToolsVersion: "26.1.0",
            minSdkVersion    : 14,
            targetSdkVersion : 28,
            versionCode      : 4,
            versionName      : "1.0.4"
    ]

    supportLibrary = "26.1.0"

    supportDependencies = [
            supportAppcompat: 'androidx.appcompat:appcompat:1.0.0',
            supportDesign   : "com.android.support:design:${supportLibrary}",
            testJunit       : "junit:junit:4.12",
            testRunner      : 'androidx.test.ext:junit:1.1.1',
            testEspresso    : 'androidx.test.espresso:espresso-core:3.1.0'
    ]
}